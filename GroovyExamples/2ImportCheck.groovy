import groovy.xml.MarkupBuilder

class ImportCheck {
    static void main(String[] args) {
        // defining a xml variable
        def xmlVariable = new MarkupBuilder()
        println(xmlVariable)
    }
}
